[package]
name = "beatbyte"
description = "BeatByte — an original 8-bit rhythm game."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
beatbyte-game = { workspace = true }
bevy = { workspace = true }

[dev-dependencies]
# For the documentation-consistency tests: the gameplay rules document
# quotes concrete numbers, and those numbers live here.
beatbyte-core = { workspace = true }
# For the rock regression gate: it renders the built-in songs, runs
# the real analysis and generation on them, and hashes the result.
beatbyte-audio = { workspace = true }
beatbyte-chart = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true
